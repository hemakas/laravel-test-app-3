{
    "private": true,
    "scripts": {
        "dev": "vite",
        "build": "composer install && npm install && npm run build && php artisan optimize && php artisan migrate --force"
    },
    "devDependencies": {
        "axios": "^1.1.2",
        "laravel-vite-plugin": "^0.7.2",
        "lodash": "^4.17.19",
        "postcss": "^8.1.14",
        "vite": "^4.0.0"
    }
}
